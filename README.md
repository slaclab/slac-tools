# slac-tools
Python tools for SLAC accelerator device control (PV based), and basic physics measurements.

## Testing
- Run tests with `python -m pytest -v`.
- Name test files using pytest discovery conventions, e.g. `test_*.py` or `*_test.py`.
